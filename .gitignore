node_modules/
.next/
out/
*.tsbuildinfo
.env*
!.env.example
playwright-report/
test-results/

# Gas City
.beads/*
!.beads/config.yaml
!.beads/metadata.json

# Per-agent tooling scratch dirs — never check these in. AI coding
# assistants stash per-session state here and it collides badly across
# agents / machines if it ever lands in git.
.claude/
.opencode/
.openkt/
