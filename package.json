{
  "name": "@openkt/landing",
  "version": "0.1.0",
  "private": true,
  "description": "Static marketing site served from openkt.ai. Built with Next.js static export so the React + Tailwind landing tree from services/web ports across verbatim. Decoupled from the dashboard at app.openkt.ai so the marketing surface stays up even when the application is deployed, restarted, or scaled.",
  "scripts": {
    "dev": "next dev -p 3100",
    "build": "next build",
    "start": "next start -p 3100",
    "preview": "npx --yes serve@14 out -l 3100",
    "lint": "eslint",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "next": "16.2.4",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.2.4",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
